apiVersion: v1
kind: Service
metadata:
  name: ms-backend-svc
  namespace: dev-backend
spec:
  type: LoadBalancer
  ports:
  - name: ms-backend-svc-8882
    port: 8882
    targetPort: 8000
    protocol: TCP
  selector:
    app: ms-backend-dp

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-backend-dp
  namespace: dev-backend
  labels:
    app: ms-backend-dp
spec:
  selector:
    matchLabels:
      app: ms-backend-app
  template:
    metadata:
      labels:
        app: ms-backend-app
    spec:
      containers:
      - name: ms-backend-app
        image: python:latest
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: "250m"
            memory: "256Mi"
          limits:
            cpu: "1"
            memory: "1Gi"
      imagePullSecrets:
      - name: docker-secret
      restartPolicy: Always
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate